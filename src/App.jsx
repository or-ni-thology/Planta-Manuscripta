import HortusGrammaticus from "./HortusGrammaticus.jsx";

/* The whole app is the plate. Nothing else lives here on purpose — this file
   is just the frame the herbarium hangs in, exactly as Mooring and the
   Persicaria Portal do it. */
export default function App() {
  return <HortusGrammaticus />;
}
